{
  "name": "@d-claassen/monorepo",
  "private": true,
  "scripts": {
    "graph": "nx graph",
    "version": "lerna version --conventional-commits --no-git-tag-version --no-push"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.8",
    "nx": "^20.0.1"
  },
  "version": "0.0.0"
}
